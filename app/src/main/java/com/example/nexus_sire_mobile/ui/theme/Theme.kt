package com.example.nexus_sire_mobile.ui.theme

import androidx.compose.material3.darkColorScheme
import androidx.compose.material3.lightColorScheme

// Defina as cores que serão usadas no tema
private val DarkColorScheme = darkColorScheme(
    primary = Purple80,
    secondary = PurpleGrey80,
    tertiary = Pink80
)

private val LightColorScheme = lightColorScheme(
    primary = Purple40,
    secondary = PurpleGrey40,
    tertiary = Pink40
)

// Renomeando a tipografia para evitar o conflito com a tipografia do Material3
val CustomTypography = androidx.compose.material3.Typography(
    // Aqui você pode definir as fontes padrão para seu tema
)

